// Product interface representing product objects.

export interface Product {
    productId: number;
    productName: string;
    productDesc: string;
    productImageUrl: string;
    productPrice: number;
}